let arr: string[] = ['onizuka'];
let teacher: (string | number | boolean)[] = ['onizuka', 24]

arr.push('teacher');
teacher.push(40);

console.log(`check arr ${arr}`);
console.log(`check teacher ${teacher}`);
